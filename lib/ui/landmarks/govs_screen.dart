import 'package:flutter/material.dart';

class GovernmentsScreen extends StatelessWidget {
  const GovernmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Governments Screen'),
    );
  }
}
